[package]
name = "virtual-wasi-clocks"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.23", default-features = false, features = ["macros", "realloc"] }

[lints]
workspace = true
