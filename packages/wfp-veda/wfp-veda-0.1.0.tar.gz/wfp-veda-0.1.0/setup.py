import setuptools 
import pathlib

setuptools.setup(
    name='wfp-veda',
    version='0.1.0',
    packages=setuptools.find_packages(),
    description = " Test lib",
    install_requires=[
        # list your dependencies here
    ],
)
