[project]
name = "illumind"
version = "0.0.0-1"
readme = "README.md"
authors = [
    { name="Benjamin N. Zelnick" },
    { name="The Children's Hospital of Philadelphia" }
]
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["illumind"]
