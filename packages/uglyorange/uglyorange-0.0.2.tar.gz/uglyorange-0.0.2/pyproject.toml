[tool.poetry]
name = "uglyorange"
version = "0.0.2"
description = ""
authors = ["ultrasev"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
fire = "^0.6.0"

[tool.poetry.scripts]
mp = "uglyorange.network:FastPing.entrypoint"
cufo = "uglyorange.network:Cufo.entrypoint"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
