[tool.poetry]
name = "uglyorange"
version = "0.0.1"
description = ""
authors = ["ultrasev"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
fire = "^0.6.0"

[tool.poetry.scripts]
mp = "uglyorange.network:fastping"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
