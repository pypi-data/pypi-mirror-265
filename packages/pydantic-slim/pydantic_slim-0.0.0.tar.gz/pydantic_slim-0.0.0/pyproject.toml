[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pydantic-slim"
version = "0.0.0"
description = "This is a placeholder in case we want to use this package name in future."
authors = [{ name = "Pydantic Team", email = "hello@pydantic.dev" }]
license = "MIT"
readme = "README.md"
classifiers = [
    "Development Status :: 1 - Planning",
    "Topic :: Internet",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/pydantic/pydantic"
