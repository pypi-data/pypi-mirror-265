C     -*- fortran -*-
C     This file is autogenerated with f2py (version:1.25.2)
C     It contains Fortran 77 wrappers to fortran functions.

      subroutine f2pywrapcchi (cchif2pywrap, v)
      external cchi
      real*4 v(*)
      real cchif2pywrap
      real  cchi
      cchif2pywrap = cchi(v)
      end


      subroutine f2pywrapfctnlg (fctnlgf2pywrap, n)
      external fctnlg
      integer n
      real fctnlgf2pywrap
      real  fctnlg
      fctnlgf2pywrap = fctnlg(n)
      end


      subroutine f2pywrapcbchi (cbchif2pywrap, v)
      external cbchi
      real v(*)
      real cbchif2pywrap
      real  cbchi
      cbchif2pywrap = cbchi(v)
      end


      subroutine f2pyinitmoddat(setupfunc)
      external setupfunc
      integer ng
      integer ntc
      real xin(4096)
      real yin(4096)
      real ein(4096)
      real theta(51)
      real efix
      real qavrg(51)
      common /moddat/ ng,ntc,xin,yin,ein,theta,efix,qavrg
      call setupfunc(ng,ntc,xin,yin,ein,theta,efix,qavrg)
      end

      subroutine f2pyinitmodfiles(setupfunc)
      external setupfunc
      character*140 filein
      integer l_file
      character*140 fileout1
      character*140 fileout2
      character*140 fileout3
      common /modfiles/ filein,l_file,fileout1,fileout2,fileout3
      call setupfunc(filein,l_file,fileout1,fileout2,fileout3)
      end

      subroutine f2pyinitoptions(setupfunc)
      external setupfunc
      integer o_el
      integer o_bgd
      integer o_w1
      character*1 prog
      common /options/ o_el,o_bgd,o_w1,prog
      call setupfunc(o_el,o_bgd,o_w1,prog)
      end

      subroutine f2pyinitmess(setupfunc)
      external setupfunc
      integer ness
      common /mess/ ness
      call setupfunc(ness)
      end

      subroutine f2pyinitfftcom(setupfunc)
      external setupfunc
      real fres(4098)
      real fwrk(4098)
      real xj(4096)
      real twopik(2049)
      integer nfft
      common /fftcom/ fres,fwrk,xj,twopik,nfft
      call setupfunc(fres,fwrk,xj,twopik,nfft)
      end

      subroutine f2pyinitdatcom(setupfunc)
      external setupfunc
      real xdat(4096)
      real dat(4096)
      real sig(4096)
      integer ndat
      common /datcom/ xdat,dat,sig,ndat
      call setupfunc(xdat,dat,sig,ndat)
      end

      subroutine f2pyinitdintrp(setupfunc)
      external setupfunc
      integer ipdat(4096)
      real xpdat(4096)
      common /dintrp/ ipdat,xpdat
      call setupfunc(ipdat,xpdat)
      end

      subroutine f2pyinitfitcom(setupfunc)
      external setupfunc
      real fit(4096)
      real resid(4096)
      integer nfew
      real fitp(10)
      real expf(2049,6)
      common /fitcom/ fit,resid,nfew,fitp,expf
      call setupfunc(fit,resid,nfew,fitp,expf)
      end

      subroutine f2pyinitsclcom(setupfunc)
      external setupfunc
      real bscl
      real ascl
      real wscl
      real sclvec(10,2)
      real gscl
      common /sclcom/ bscl,ascl,wscl,sclvec,gscl
      call setupfunc(bscl,ascl,wscl,sclvec,gscl)
      end

      subroutine f2pyinitgrdcom(setupfunc)
      external setupfunc
      real dddpar(4096,10)
      real fr2pik(4098,2)
      common /grdcom/ dddpar,fr2pik
      call setupfunc(dddpar,fr2pik)
      end

      subroutine f2pyinitwrkcom(setupfunc)
      external setupfunc
      real work(4098,2)
      common /wrkcom/ work
      call setupfunc(work)
      end

      subroutine f2pyinitmodpars(setupfunc)
      external setupfunc
      integer nbin
      integer imin
      integer imax
      real rscl
      real bnorm
      common /modpars/ nbin,imin,imax,rscl,bnorm
      call setupfunc(nbin,imin,imax,rscl,bnorm)
      end

      subroutine f2pyinitqw1com(setupfunc)
      external setupfunc
      real qw1(51)
      real sigqw1(51)
      integer ispec
      common /qw1com/ qw1,sigqw1,ispec
      call setupfunc(qw1,sigqw1,ispec)
      end

      subroutine f2pyinitstexp(setupfunc)
      external setupfunc
      real betexp
      real xbeta(101)
      real betchi(101)
      integer nbtot
      common /stexp/ betexp,xbeta,betchi,nbtot
      call setupfunc(betexp,xbeta,betchi,nbtot)
      end


