defaults:
  - nltk_universal

stopwords:
  stopwords_fn: "lambda x: len(x) <= 2"
  # stopwords_list:
  nltk_stopwords_lang: english
  stopwords_path: ${oc.select:variables.project_root_path,.}/tests/assets/stopwords/nbcpu-tokenizer.txt
  verbose: false
lowercase: true
include_whitespace_token: false
strip_pos: false
tagger:
  stem: false
  lemmatize: true
verbose: false
