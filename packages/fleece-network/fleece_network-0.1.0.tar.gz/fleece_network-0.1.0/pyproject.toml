[tool.poetry]
name = "fleece-network"
version = "0.1.0"
description = "P2P socket built on aiortc."
authors = ["Linyu Wu <celve03@gmail.com>"]
readme = "README.md"
packages = [{ include = "peerrtc" }]

[tool.poetry.dependencies]
python = "^3.10"
aiortc2 = "^1.8.0"
uvicorn = "^0.29.0"
websockets = "^12.0"
toml = "^0.10.2"
aioconsole = "^0.7.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
