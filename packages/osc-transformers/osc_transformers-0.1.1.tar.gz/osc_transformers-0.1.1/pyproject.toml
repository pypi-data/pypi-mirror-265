[tool.poetry]
name = "osc-transformers"
version = "0.1.1"
description = "以配置文件的形式构建transformer模型"
authors = ["wangmengdi <790990241@qq.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "osc_transformers"}]

[tool.poetry.dependencies]
python = ">=3.7"
confection = "^0.1.3"
catalogue = ">=2.0.10"
torch = ">=2.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
